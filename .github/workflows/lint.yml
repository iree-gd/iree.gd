name: Lint

on:
  workflow_dispatch:
  workflow_call:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v4
      - name: Setup clang-format
        shell: bash
        run: |
          python -m pip install clang-format==22.1.0
      - name: Run clang-format
        shell: bash
        run: |
          clang-format --style=file:.clang-format src/** --dry-run --Werror
